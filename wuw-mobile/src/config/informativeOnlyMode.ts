/**
 * When true, competitions are browsing-only for App Review: no ticket selection,
 * checkout, or orders. Redirects away from question/checkout/confirmation/error routes.
 *
 * Set to false when purchases are approved and should be enabled in the mobile app.
 */
export const INFORMATIVE_ONLY_MODE = true;

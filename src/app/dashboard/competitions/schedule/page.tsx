import { CompetitionScheduleDashboard } from '@/components/admin/CompetitionScheduleDashboard';

export default function DashboardCompetitionSchedulePage() {
  return <CompetitionScheduleDashboard />;
}
